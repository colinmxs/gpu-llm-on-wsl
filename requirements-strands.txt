# Requirements for Strands Agents SDK Integration

# Core Strands Agents SDK
strands-agents>=1.0.0

# Optional Strands packages
# strands-agents-tools>=0.2.0  # Community tools package
# strands-agents-builder  # Agent builder package

# Existing dependencies (keep your current versions)
# torch
# transformers
# gradio
# humanize
# bitsandbytes

# Optional: For enhanced Strands features
# pydantic>=2.0.0  # For structured output (included with strands-agents)
# redis  # For Valkey/Redis session management (optional)
